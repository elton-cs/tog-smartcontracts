import { SmartContract } from "o1js";


export class GameState extends SmartContract {
    // one state contract for the map (map layout)
    // one state contract for the player locations
    // one state contract for the player movement
    // one state contract for the player attacks
}